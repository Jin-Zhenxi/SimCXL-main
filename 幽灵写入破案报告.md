# 幽灵写入破案报告

## 问题描述

Guest OS 通过 `/dev/mem` + mmap 向 CXL BAR 基址 `0x400000000` + 偏移 `0x10000` 写入数据，程序正常执行并触发 m5 exit 关机，但 CXLMemCtrl::CXLResponsePort::recvTimingReq 中的 DPRINTF(MatrixFlow, ...) 拦截日志**完全没有输出**。

---

## 1. C++ 编译状态 (Compilation Status)

### 1.1 文件修改时间对比

| 文件 | 最后修改时间 |
|------|-------------|
| `build/X86/gem5.opt` | 2026-03-15 **15:40:15** |
| `src/dev/x86/cxl_mem_ctrl.cc` | 2026-03-15 **16:46:59** |

**结论：可执行文件比源码旧约 1 小时 7 分钟。**

**cxl_mem_ctrl.cc 在 gem5.opt 编译之后被修改，拦截逻辑从未被重新编译进二进制。**

### 1.2 MatrixFlow Debug Flag 注册

- **位置**：`src/mem/SConscript` 第 149 行
- **内容**：`DebugFlag('MatrixFlow')`
- **结论**：MatrixFlow 已正确注册。

---

## 2. 缓存层级与截留风险 (Cache Hierarchy & Eviction)

### 2.1 配置中的 Cache 实例

配置文件 `x86-cxl-type3-with-classic.py` 使用：

```python
cache_hierarchy = PrivateL1PrivateL2SharedL3CacheHierarchy(
    l1d_size="48kB", l1i_size="32kB",
    l2_size="2MB", l3_size="96MB",
    ...
)
```

**结论：CPU 与 CXL 之间存在完整的 L1/L2/L3 缓存层级。**

### 2.2 理论分析：MMIO 是否被 Cache 截留？

- Guest 使用 `open("/dev/mem", O_RDWR | O_SYNC)` + mmap，普通指针赋值。
- 在 gem5 经典内存模型中，**未显式标记为 uncacheable 的地址** 会被当作普通可缓存内存。
- x86_board 的 `_setup_memory_ranges` 中**未**将 CXL 区域（如 0x400000000）加入 `uncacheable_range` 或 PMA 的 uncacheable 列表。
- 因此，对 `0x400010000` 的写入**可能被 L1 写 Cache 截留**，作为 Dirty 数据暂存，直到 eviction 或写回。
- 若程序在 `m5 exit` 之后很快退出，**Dirty 数据可能尚未写回到底层总线**，请求可能从未到达 CXLMemCtrl。

**结论：存在 Cache 截留风险，但需结合地址路由进一步判断。**

---

## 3. 地址路由与内存映射 (Address Routing)

### 3.1 Python 配置中的 CXL 地址范围

`x86_board.py` 第 145 行：

```python
cxl_mem_range = AddrRange(Addr(0x100000000), size=cxl_dram.get_size())
```

- 8GB CXL 内存 → 范围：`[0x100000000, 0x300000000)`
- 即物理地址 **4GB ~ 12GB** 被路由到 CXLMemCtrl。

### 3.2 实际运行时 Guest 的 CXL 地址

`board.pc.com_1.device` 启动日志：

```
pci 0000:00:06.0: BAR 0 [mem 0x400000000-0x5ffffffff 64bit]: assigned
```

- 内核实际分配的 CXL BAR 基址（物理地址）：**0x400000000**（16GB）
- Guest 写入目标：**0x400000000 + 0x10000 = 0x400010000**

### 3.3 桥接器 (CXLBridge) 的 ranges

`output/fs_auto/config.ini` 中 board.bridge 的 ranges：

```
ranges=3221225472:4294901760 9223372036854775808:11529215046068469759 
       13835058055282163712:18446744073709551615 4294967296:12884901888
```

解码后：

- `4294967296:12884901888` = `0x100000000:0x300000000` → **CXL 内存范围**

**结论：**

- 桥接器只转发 `[0x100000000, 0x300000000)` 的请求到 CXL。
- 目标地址 `0x400010000` **不在该范围内**。
- 请求会被桥接器的 default 端口处理（如 bad_addr），**不会到达 CXLMemCtrl**。

---

## 4. 最终结论与嫌疑排序

| 嫌疑点 | 严重程度 | 说明 |
|--------|----------|------|
| **1. 未重新编译** | **高** | gem5.opt 比 cxl_mem_ctrl.cc 旧，拦截逻辑未包含在二进制中，DPRINTF 不可能执行 |
| **2. 地址路由错误** | **高** | 配置中 CXL 地址为 0x100000000，Guest 实际使用 0x400000000，请求未路由到 CXLMemCtrl |
| **3. Cache 截留** | **中** | 若地址正确，CXL 区域未标记 uncacheable，仍有被 L1 截留的可能 |

---

## 5. 最大嫌疑点

**最大嫌疑点是：未重新编译 + 地址路由错误。**

1. **未重新编译**：即使请求正确到达 CXLMemCtrl，当前运行的 gem5.opt 中也没有拦截逻辑，DPRINTF 不可能输出。
2. **地址路由错误**：Guest 写入 0x400010000，但桥接器只转发 0x100000000~0x300000000，请求根本不会到达 CXLMemCtrl。

---

## 6. 建议修复顺序

1. **立即重新编译**：`scons build/X86/gem5.opt -j$(nproc)`，确保 cxl_mem_ctrl.cc 的修改被编译进二进制。
2. **修正地址路由**：将 `cxl_mem_range` 的基址改为 0x400000000，或调整桥接器 ranges，使 0x400000000~0x600000000 被正确路由到 CXLMemCtrl。
3. **可选：标记 CXL MMIO 为 uncacheable**：在 x86_board 的 PMA 或 uncacheable 配置中，将 CXL BAR 区域（如 0x400000000）标记为 uncacheable，避免 Cache 截留。
